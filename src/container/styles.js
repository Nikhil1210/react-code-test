export default theme => ({
    content: {
        alignItems: 'center',
        display: 'flex'
    },
    root: {
        margin: theme.spacing(2),
        minHeight: theme.spacing(20),
        minWidth: theme.spacing(30),
        paddingTop: 0
    }
})